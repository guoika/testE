import { Routes } from '@angular/router';
import { SubcriptionComponent } from './subcription.component';

export const SubcriptionRoutes: Routes = [
    {
        path: '',
        component: SubcriptionComponent,
    }
];
